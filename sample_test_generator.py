import datetime

current_utc = datetime.datetime.now().strftime("%Y-%m-%d, %H:%M:%S")
_MAC =
_type =

_rssi =
_vbat =
_temp =
_humi =
_payload = 


record = {
    "_id" : _MAC,
    "type" : _type,
    "test_resuilts" : [ {
        "datetime" : current_utc,[ {
            "rssi" : _rssi,

    ]
    

